# Development only —— the source is mounted and only the dependencies live in the image.
FROM oven/bun:1-alpine
WORKDIR /app
COPY package.json bun.lock* ./
RUN bun install --frozen-lockfile
EXPOSE 5173
CMD ["bun", "run", "dev", "--host", "0.0.0.0"]
