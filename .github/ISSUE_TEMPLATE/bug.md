---
name: Bug
about: I ran it and it behaved differently from what I expected
labels: bug
---

## What did you run

```bash
# the exact command you typed
```

## What came out

```
# the output verbatim. Do not summarize — summaries delete the cause.
```

## What did you expect

## Environment

Paste the output of `just status`. If that does not run, fill this in by hand:

- OS · architecture:
- `uv --version` · `python -V`:
- Docker (`just up`) or host:
- Number of documents in the vault (roughly):

> ⚠ **Do not paste secrets or personal note content.** Leave `.env` out entirely, and redact
> paths where needed.
